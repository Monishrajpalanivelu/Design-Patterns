package Behavioral_Patterns.Command_Pattern;

public interface CommandOperation { // this is the command interface 
      public void execute(); // it has the execute method 
      // all the command objects will have this method 
      // and it will call the reciever object method 
      
}
