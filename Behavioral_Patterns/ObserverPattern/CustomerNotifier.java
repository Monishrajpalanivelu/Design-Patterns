package Behavioral_Patterns.ObserverPattern;

// Observer interface (subscriber)
public interface CustomerNotifier {
      
      public void notify(String product); // shows the notification
      

}
